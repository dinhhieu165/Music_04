<div class="col-md-2 w3-light-grey">
<h2><%= t "layouts.categories" %></h2>
    <% all_categories.each do |category| %>
      <h4 class="">
        <%= link_to category.title, "#" %>
      </h4>
    <% end %>
</div>
