<% provide(:title, t("admin.artists.edit_title")) %>
<% provide(:button_text, t("admin.artists.edit_submit")) %>
<h1><%= t "admin.artists.update" %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
